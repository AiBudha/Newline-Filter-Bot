services:
  - type: web
    name: New-Line-Movies
    env: python
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: free
    region: oregon
    healthCheckPath: /

#End of yaml
